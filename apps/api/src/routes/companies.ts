import type { FastifyInstance, FastifyPluginOptions } from "fastify";

export async function companiesRoutes(
  _app: FastifyInstance,
  _opts: FastifyPluginOptions
) {
  // CRUD companies — à implémenter
}

# Copier ce fichier en .env.local pour tester l'app web en local
# Les valeurs ci-dessous suffisent pour lancer le front sans backend.

NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Optionnel : URL de l'API si vous lancez aussi le backend (pnpm dev:api)
# API_URL="http://localhost:4000"
